%h2 Feedback Questions for #{@course_type.title} 
%table.table.table-bordered.table-striped.table-hover
    %th Question
    %th Type
    %th{colspan:3}

    -@course_type.feedback_questions.each do |question|
        %tr
            %td= question.content
            %td= question.question_type
            %td= link_to "Show", [:admin, @course_type, question]
            %td= link_to "Edit", edit_admin_course_type_feedback_question_path(@course_type, question)
            %td= link_to "Destroy", [:admin, @course_type, question], method: :delete
    -if @course_type.feedback_questions.empty?
        %tr
            %td{colspan: 12, style: "text-align:center;padding:20px"} There are currently no feedback questions for this course.

%p= link_to "Add Question", new_admin_course_type_feedback_question_path(@course_type), class: "btn btn-primary"
= link_to 'View the course',  admin_course_type_path(@course_type)



