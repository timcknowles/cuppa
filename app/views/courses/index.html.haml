%h1 Listing courses

%table.table.table-striped.table-hover
  %tr
    %th Title
    %th Date
    %th Start time
    %th End time
    %th Venue
    %th Description
    %th Price
    %th Places available
    %th Number Registered
    %th PLaces Left

  - @courses.each do |course|
    %tr
      %td= course.title
      %td= course.date
      %td= course.start_time
      %td= course.end_time
      %td= course.venue
      %td= course.description
      %td= course.price
      %td= course.places_available
      %td= pluralize course.users.size, 'place'
      %td= pluralize (places_available=-course.users.size), 'place'
      %td= link_to 'Show', course
      %td= link_to 'Edit', edit_course_path(course)
      %td= link_to 'Destroy', course, :method => :delete, :data => { :confirm => 'Are you sure?' }
           
      -if course.users.size > 2 
        %td= link_to "Register", new_user_path(@user)
      -else
        %td= link_to "Add me to the waiting list", new_user_path(@user)


         
      
%br

= link_to 'New Course', new_course_path
