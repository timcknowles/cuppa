%h1 New user

= form_for @user do |f|

    -if @course
        %h2 You are registering on #{@course.title}
        =hidden_field_tag :course_id, @course.id

    = render 'form', f: f

= link_to 'Back', users_path